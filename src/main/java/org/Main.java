package org;

import org.BotRoutePlanner;

public class Main {
    public static void main(String[] args) {
        BotRoutePlanner botRoutePlanner = new BotRoutePlanner();
        botRoutePlanner.solve(args);
    }
}
